-----

# คู่มือการใช้งานโปรแกรมตรวจสอบความถูกต้อง P\&L (Enhanced P\&L Reconciliation Tool)

## 1\. ภาพรวมของโปรแกรม (Overview)

โปรแกรมนี้ถูกออกแบบมาเพื่อ **Automate (ทำอัตโนมัติ)** กระบวนการตรวจสอบความถูกต้อง (Reconciliation) ของรายงานงบกำไรขาดทุน (P\&L Report) โดยเปรียบเทียบข้อมูลระหว่าง 3 แหล่งข้อมูลหลัก ได้แก่:

1.  **Source File (CSV):** ข้อมูลดิบจากระบบฐานข้อมูล
2.  **Excel Report (XLSX):** รายงาน P\&L ที่จัดทำเสร็จแล้ว
3.  **Financial Statement (Text File):** งบการเงินฉบับทางการ

โปรแกรมรองรับการตรวจสอบทั้งแบบ **รายเดือน (MTH)** และ **สะสมรายปี (YTD)** พร้อมกัน

-----

## 2\. สิ่งที่ต้องเตรียม (Prerequisites)

### 2.1 สภาพแวดล้อม (Environment)

  * ติดตั้ง **Python 3.x**
  * ติดตั้ง Library ที่จำเป็น:
    ```
    pip install pandas openpyxl argparse
    ```

### 2.2 การเตรียมไฟล์ (File Preparation)

โปรแกรมจะค้นหาไฟล์ในโฟลเดอร์เดียวกับที่สคริปต์อยู่ โดยต้องตั้งชื่อไฟล์ตามรูปแบบดังนี้ (สมมติวันที่ตรวจสอบคือ **31 ต.ค. 2025** และบริษัท **NT**):

| ประเภทไฟล์ | รูปแบบชื่อไฟล์ (Naming Convention) | ตัวอย่างไฟล์จริง |
| :--- | :--- | :--- |
| **Excel รายเดือน** | `Report_{COMPANY}_{YYYYMM}.xlsx` | `Report_NT_202510.xlsx` |
| **Excel สะสม** | `Report_{COMPANY}_{YYYY}.xlsx` | `Report_NT_2025.xlsx` |
| **Source Cost** | `TRN_PL_COSTTYPE_{COMPANY}_{PERIOD}_TABLE_{DATE}.csv` | `TRN_PL_COSTTYPE_NT_MTH_TABLE_20251031.csv` |
| **Source GL** | `TRN_PL_GLGROUP_{COMPANY}_{PERIOD}_TABLE_{DATE}.csv` | `TRN_PL_GLGROUP_NT_MTH_TABLE_20251031.csv` |
| **Text File** | `pld_{company}_{date}.txt` | `pld_nt_20251031.txt` |

> **หมายเหตุ:** ภายในไฟล์ Excel ต้องมีชื่อ Sheet ตามที่กำหนด (เช่น `ต้นทุน_กลุ่มธุรกิจ`, `หมวดบัญชี_กลุ่มธุรกิจ` ฯลฯ) หากชื่อไม่ตรง โปรแกรมจะแจ้งเตือนว่าโหลด Sheet ไม่ได้

-----

## 3\. วิธีการใช้งาน (How to Run)

เปิด Command Prompt หรือ Terminal แล้วรันคำสั่งตามรูปแบบด้านล่าง:

### 3.1 การรันแบบมาตรฐาน (แนะนำ)

ระบุวันที่ที่ต้องการตรวจสอบ เพื่อให้โปรแกรมดึงไฟล์ได้ถูกต้องอัตโนมัติ

```bash
python pl_reconciliation.py --date 20251031
```

### 3.2 การรันแบบระบุระดับการตรวจสอบ (Validation Mode)

หากต้องการเปลี่ยนความละเอียดในการตรวจสอบ สามารถใช้ parameter `--mode`:

  * `basic`: ตรวจสอบเฉพาะกำไรสุทธิ (Net Profit)
  * `enhanced`: (ค่าเริ่มต้น) ตรวจสอบ รายได้, ค่าใช้จ่าย, กำไรสุทธิ และสูตรคณิตศาสตร์พื้นฐาน
  * `comprehensive`: ตรวจสอบทุกจุด (เตรียมไว้สำหรับฟีเจอร์ในอนาคต)

**ตัวอย่าง:**

```bash
python pl_reconciliation.py --date 20251031 --mode basic
```

### 3.3 การรันแบบระบุบริษัท (Company Code)

หากต้องการตรวจสอบบริษัทอื่นที่ไม่ใช่ค่าเริ่มต้น (NT):

```bash
python pl_reconciliation.py --date 20251031 --company MYCOMP
```

-----

## 4\. หลักการทำงานและการตรวจสอบ (Validation Logic)

โปรแกรมแบ่งการตรวจสอบออกเป็น 3 ระดับ (Level) ดังนี้:

### 📊 ระดับที่ 1: ความครบถ้วน (Completeness)

**เป้าหมาย:** ยืนยันว่าข้อมูลจาก Source (CSV) ถูกนำมาลงใน Report (Excel) ครบถ้วน ไม่ตกหล่น

  * **1.1 Cost Type Check:** เปรียบเทียบ `รายได้` และ `กำไรสุทธิ` ระหว่าง CSV Cost Type กับ Excel Sheet (ต้นทุน\_กลุ่มธุรกิจ)
  * **1.2 GL Group Check:** เปรียบเทียบ `รายได้` และ `กำไรสุทธิ` ระหว่าง CSV GL Group กับ Excel Sheet (หมวดบัญชี\_กลุ่มธุรกิจ)

### 🔄 ระดับที่ 2: ความสอดคล้อง (Consistency)

**เป้าหมาย:** ยืนยันว่า Report ทั้ง 2 มุมมอง (Cost Type และ GL Group) ให้ตัวเลขที่เท่ากัน

  * **2a-2c Cross-Sheet:** เปรียบเทียบตัวเลขข้าม Sheet ระหว่าง Cost Type vs GL Group
      * Sheet ธุรกิจ vs ธุรกิจ
      * Sheet กลุ่มบริการ vs กลุ่มบริการ
      * Sheet บริการ vs บริการ
  * **2d Internal Math (Enhanced Mode):** ตรวจสอบสูตรคำนวณภายใน Excel ว่าถูกต้องหรือไม่
      * *สูตร:* `รายได้รวม - ค่าใช้จ่ายรวม - ภาษี = กำไรสุทธิ`
      * หาก Excel มีการ Hardcode ตัวเลขมาผิด สูตรนี้จะจับผิดได้ทันที

### ✓ ระดับที่ 3: Tie-out กับงบการเงิน

**เป้าหมาย:** ยืนยันว่าตัวเลขใน Excel ตรงกับงบการเงินฉบับทางการ (Text File)

  * ดึงตัวเลข `กำไรสุทธิ` จากไฟล์ Text (งบการเงิน) มาเทียบกับ Excel (หมวดบัญชี\_กลุ่มธุรกิจ)

-----

## 5\. การอ่านผลลัพธ์ (Reading the Output)

เมื่อโปรแกรมทำงานเสร็จ จะแสดงตารางสรุปผล:

```text
====================================================================================================
                                      สรุปผลการตรวจสอบ                                      
====================================================================================================
รายการตรวจสอบ                                      |        ส่วนต่าง | สถานะ       
====================================================================================================
1.1a Revenue: Source Cost vs Report                |            0.00 | ✅ PASS
1.1b Net Profit: Source Cost vs Report             |            0.00 | ✅ PASS
2a. Cross-Sheet Revenue: กลุ่มธุรกิจ                  |            0.00 | ✅ PASS
2d. Internal Math: Cost Type - กลุ่มธุรกิจ            |     -5,000.00 | ❌ FAIL
   └─ Revenue - Expense - Tax = Net Profit ใน Cost Type - กลุ่มธุรกิจ
   └─ Revenue - Expense - Tax: 1,500,000.00
   └─ Net Profit (in report): 1,505,000.00
...
```

  * **✅ PASS:** ส่วนต่าง (Difference) น้อยกว่าค่า Tolerance (0.001) -\> ข้อมูลถูกต้อง
  * **❌ FAIL:** ส่วนต่างเกินกว่าที่กำหนด -\> ข้อมูลผิดปกติ
      * โปรแกรมจะแสดงรายละเอียดบรรทัดล่างว่า **ค่าที่คำนวณได้** คือเท่าไร และ **ค่าใน Report** คือเท่าไร เพื่อให้ผู้ใช้ง่ายต่อการหาสาเหตุ

-----


## 6\. การแก้ไขปัญหาและข้อผิดพลาด (Troubleshooting Guide)

หากโปรแกรมทำงานไม่สมบูรณ์ หรือแสดงผลลัพธ์ **FAIL** ให้ตรวจสอบตามอาการและ Error Code ดังต่อไปนี้

### 6.1 ปัญหาเรื่องไฟล์และการอ่านข้อมูล (File & Loading Issues)

#### ❌ Error: `FileNotFoundError: [Errno 2] No such file or directory...`

  * **อาการ:** โปรแกรมหยุดทำงานทันทีและแจ้งว่าหาไฟล์ไม่เจอ
  * **สาเหตุ:**
    1.  วางไฟล์ **คนละโฟลเดอร์** กับตัว Script (`pl_reconciliation.py`)
    2.  ตั้งชื่อไฟล์ **ไม่ตรงตามรูปแบบ** ที่กำหนด หรือระบุวันที่ (`--date`) ผิด
    3.  นามสกุลไฟล์ผิด (เช่น `.xls` แทนที่จะเป็น `.xlsx` หรือ `.csv` เป็น `.txt`)
  * **วิธีแก้ไข:**
      * ตรวจสอบคำสั่งรันว่าวันที่ตรงกับชื่อไฟล์จริงหรือไม่ (เช่น `--date 20251031`)
      * ตรวจสอบชื่อไฟล์ในโฟลเดอร์ให้ตรงกับ Pattern ในคู่มือข้อ 2.2 ทุกตัวอักษร

#### ❌ Error: `UnicodeDecodeError: 'charmap' codec can't decode byte...` หรือ อ่านแล้วเป็นภาษาต่างดาว

  * **อาการ:** โปรแกรมแจ้ง Error เกี่ยวกับ Codec หรืออ่านค่าออกมาเป็นตัวอักษรขยะ
  * **สาเหตุ:** ไฟล์ CSV หรือ Text ถูกบันทึกมาในรูปแบบ **UTF-8** แต่ Script ตั้งค่าไว้อ่าน **CP874 (Windows Thai)** (หรือกลับกัน)
  * **วิธีแก้ไข:**
      * **วิธีที่ 1 (แก้ที่ไฟล์):** เปิดไฟล์ CSV/Text ใน Notepad -\> เลือก Save As -\> ตรง Encoding เลือก `ANSI` (สำหรับ CP874) หรือ `UTF-8` ให้ตรงกับโค้ด
      * **วิธีที่ 2 (แก้ที่โค้ด):** ค้นหาบรรทัด `encoding='cp874'` ใน Script และเปลี่ยนเป็น `encoding='utf-8'`

-----

### 6.2 ปัญหาเรื่องข้อมูลไม่ตรง (Data Validation Failures)

#### ❌ สถานะ: `FAIL` (แต่ตัวเลขต่างกันมหาศาล หรือเป็น 0)

  * **อาการ:** โปรแกรมอ่านค่าได้ `0.00` ทั้งที่มีตัวเลขในไฟล์ หรือหาค่าไม่เจอ
  * **สาเหตุ:** **Keywords (คำค้นหา)** ในไฟล์ Excel เปลี่ยนไปจากเดิม
      * *ตัวอย่าง:* โค้ดค้นหาคำว่า `"รายได้รวม"` แต่ใน Excel เปลี่ยนเป็น `"รวมรายได้ทั้งหมด"` หรือ `"Total Revenue"`
  * **วิธีแก้ไข:**
      * เปิดไฟล์ Excel ไปยัง Sheet ที่มีปัญหา
      * ดูชื่อรายการ (Description) ในคอลัมน์ A หรือ B
      * แก้โค้ด Python ในฟังก์ชัน `extract_values` ตรงส่วน `keywords` ให้ตรงกับข้อความใน Excel
    <!-- end list -->
    ```python
    # ตัวอย่างการแก้
    # เดิม: revenue = get_val_from_df(df, ['รายได้รวม'])
    # ใหม่: revenue = get_val_from_df(df, ['รวมรายได้สุทธิ'])
    ```

#### ❌ สถานะ: `FAIL` (ส่วนต่างเป็นตัวเลขเท่าเดิมซ้ำๆ เช่น Diff = 50.00 ทุก Sheet)

  * **อาการ:** ผลต่างคงที่ในหลายๆ จุดตรวจสอบ
  * **สาเหตุ:** มักเกิดจาก **การปัดเศษ (Rounding)** หรือ **Signage (เครื่องหมายบวก/ลบ)**
      * ในระบบหนึ่ง ค่าใช้จ่ายอาจเก็บเป็น **ลบ (-)** แต่อีกระบบเก็บเป็น **บวก (+)**
  * **วิธีแก้ไข:**
      * ตรวจสอบค่า `tolerance` ในโค้ด (ปัจจุบันตั้งไว้ 0.001) หากส่วนต่างเกิดจากการปัดเศษทศนิยมจริงๆ อาจเพิ่มค่านี้ได้
      * ตรวจสอบสูตรในโค้ดว่าต้อง `abs()` (ทำเป็นค่าสัมบูรณ์) หรือไม่

-----

### 6.3 ปัญหาโครงสร้างไฟล์ (Structure Mismatch)

#### ❌ Warning: `ไม่สามารถโหลด sheet 'ชื่อ_Sheet': ...`

  * **อาการ:** โปรแกรมรันจนจบ แต่ค่าในส่วนของ Sheet นั้นเป็น 0 ทั้งหมด
  * **สาเหตุ:** **ชื่อ Tab (Sheet Name)** ในไฟล์ Excel ไม่ตรงกับที่ Config ไว้
  * **วิธีแก้ไข:**
      * เปิดไฟล์ Excel เช็คชื่อ Tab ด้านล่าง
      * แก้ไขชื่อ Tab ให้ตรงกับ Config หรือ ไปแก้ที่ `FileConfig` ในโค้ด:
    <!-- end list -->
    ```python
    self.sheets = {
        'cost_biz': 'ต้นทุน_กลุ่มธุรกิจ', # <-- แก้ชื่อตรงนี้ให้ตรงกับ Excel
        ...
    }
    ```

#### ❌ ค่าที่อ่านได้ผิดบรรทัด หรือผิดคอลัมน์

  * **อาการ:** อ่านค่า "รายได้" ไปเป็นตัวเลขวันที่ หรือตัวเลขอื่นที่ไม่เกี่ยวข้องกัน
  * **สาเหตุ:** มีการแทรกคอลัมน์เพิ่มใน Excel ทำให้ `col_index` เลื่อน
  * **วิธีแก้ไข:**
      * ในฟังก์ชัน `get_val_from_df` ค่า Default คือ `col_index=2` (คือคอลัมน์ C, นับจาก 0=A, 1=B, 2=C)
      * ถ้าย้ายคอลัมน์ตัวเลข ให้ระบุ `col_index` ใหม่ตอนเรียกใช้ฟังก์ชัน

-----

### 6.4 ปัญหาทางเทคนิค (Environment)

#### ❌ Error: `ModuleNotFoundError: No module named 'pandas'`

  * **สาเหตุ:** ยังไม่ได้ติดตั้ง Library ที่จำเป็น
  * **วิธีแก้ไข:** รันคำสั่ง `pip install pandas openpyxl` ใน Command Prompt

-----

### สรุปขั้นตอนการแจ้งปัญหา (Escalation Process)

หากแก้ไขเบื้องต้นแล้วยังไม่หาย ให้รวบรวมข้อมูลดังนี้ส่งให้ทีม Developer:

1.  **Screenshot** หน้าจอ Error หรือผลลัพธ์ที่ FAIL
2.  ไฟล์ **Report Excel** ที่ใช้ตรวจสอบ
3.  ไฟล์ **Source CSV/Text** ที่ใช้
4.  ระบุ **Command** ที่ใช้รัน (เช่น รันด้วยวันที่อะไร)

-----
