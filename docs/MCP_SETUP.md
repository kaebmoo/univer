# MCP Server Setup Guide
## คู่มือการติดตั้งและใช้งาน MCP Server สำหรับ Univer Report System

## 📖 MCP (Model Context Protocol) คืออะไร?

Model Context Protocol (MCP) เป็นโปรโตคอลมาตรฐานที่พัฒนาโดย Anthropic สำหรับการเชื่อมต่อระหว่าง AI agents (เช่น Claude Desktop) กับแหล่งข้อมูลและเครื่องมือต่างๆ ของคุณ

**ประโยชน์ของ MCP:**
- ให้ Claude Desktop เข้าถึงข้อมูล P&L Report ของคุณได้โดยตรง
- คุณสามารถถามคำถามเกี่ยวกับข้อมูลทางการเงินผ่าน Claude Desktop
- Claude สามารถคำนวณ metrics ทางการเงินและวิเคราะห์ข้อมูลให้คุณได้
- ไม่ต้อง copy-paste ข้อมูลเข้าไปใน Claude ด้วยตัวเอง

## 🔧 Prerequisites

1. **Claude Desktop Application**
   - ดาวน์โหลดและติดตั้ง Claude Desktop จาก [claude.ai](https://claude.ai/download)
   - ต้องใช้บัญชี Claude Pro หรือ Claude Team

2. **Python Environment**
   - Python 3.8 ขึ้นไป
   - Virtual environment ที่ติดตั้ง dependencies แล้ว (จาก `requirements.txt`)

3. **ข้อมูล CSV**
   - `backend/data/profit_loss.csv`
   - `backend/data/other_income_expense.csv`

## ⚙️ การติดตั้งและตั้งค่า

### Step 1: ตรวจสอบการติดตั้ง MCP SDK

MCP SDK ควรติดตั้งแล้วใน virtual environment (ดูที่ `backend/requirements.txt`):

```bash
cd /home/user/univer/backend
source venv/bin/activate
pip list | grep mcp
```

ควรเห็น: `mcp 1.20.0` หรือเวอร์ชันใหม่กว่า

### Step 2: ทดสอบ MCP Server

ทดสอบว่า MCP server สามารถรันได้:

```bash
cd /home/user/univer/backend
./start_mcp_server.sh
```

หากรันสำเร็จ server จะรออยู่ในโหมด stdio (ไม่มี output) - กด Ctrl+C เพื่อหยุด

### Step 3: ตั้งค่า Claude Desktop Configuration

ค้นหาไฟล์ config ของ Claude Desktop ตามระบบปฏิบัติการ:

**macOS:**
```
~/Library/Application Support/Claude/claude_desktop_config.json
```

**Windows:**
```
%APPDATA%\Claude\claude_desktop_config.json
```

**Linux:**
```
~/.config/Claude/claude_desktop_config.json
```

### Step 4: แก้ไข Configuration File

เปิดไฟล์ `claude_desktop_config.json` และเพิ่ม MCP server configuration:

```json
{
  "mcpServers": {
    "univer-report": {
      "command": "/home/user/univer/backend/start_mcp_server.sh",
      "args": [],
      "env": {}
    }
  }
}
```

**สำคัญ:** เปลี่ยน `/home/user/univer` ให้เป็น path จริงที่โปรเจคของคุณอยู่

**ตัวอย่างสำหรับ macOS/Linux:**
```json
{
  "mcpServers": {
    "univer-report": {
      "command": "/Users/yourname/projects/univer/backend/start_mcp_server.sh",
      "args": [],
      "env": {}
    }
  }
}
```

**ตัวอย่างสำหรับ Windows:**
```json
{
  "mcpServers": {
    "univer-report": {
      "command": "C:\\Users\\YourName\\univer\\backend\\venv\\Scripts\\python.exe",
      "args": ["C:\\Users\\YourName\\univer\\backend\\mcp_server\\server.py"],
      "env": {}
    }
  }
}
```

### Step 5: Restart Claude Desktop

ปิดและเปิด Claude Desktop ใหม่เพื่อโหลด configuration

## 🛠️ Available Tools

MCP Server นี้มี 3 tools ที่ Claude สามารถใช้งานได้:

### 1. `get_report_data`

ดึงข้อมูล P&L Report พร้อมกรองตามเงื่อนไข

**Parameters:**
- `year` (required): ปีที่ต้องการ (2020-2030)
- `months` (required): รายการเดือน [1-12], เช่น [1,2,3] สำหรับ Q1
- `business_groups` (optional): กลุ่มธุรกิจที่ต้องการ, ถ้าไม่ระบุจะรวมทุกกลุ่ม

**ตัวอย่างการใช้งาน:**
```
สร้างรายงาน P&L สำหรับปี 2024 เดือน 1-3 (Q1) ของกลุ่มธุรกิจ Mobile
```

### 2. `get_filter_options`

ดึงข้อมูลตัวเลือกที่มีอยู่ในระบบ (ปี, กลุ่มธุรกิจ, บริการ)

**Parameters:** ไม่มี

**ตัวอย่างการใช้งาน:**
```
มีข้อมูลปีไหนบ้างในระบบ?
มีกลุ่มธุรกิจอะไรบ้าง?
```

### 3. `calculate_metrics`

คำนวณตัวชี้วัดทางการเงิน (EBIT, EBITDA, profit margins, common size)

**Parameters:**
- `revenue` (required): รายได้รวม
- `cost_of_service` (required): ต้นทุนบริการ
- `selling_expenses` (required): ค่าใช้จ่ายขายและการตลาด
- `admin_expenses` (required): ค่าใช้จ่ายบริหาร
- `depreciation` (optional): ค่าเสื่อมราคา (default: 0)
- `amortization` (optional): ค่าตัดจำหน่าย (default: 0)

**ตัวอย่างการใช้งาน:**
```
คำนวณ EBIT และ EBITDA margin จากรายได้ 1,000,000
ต้นทุนบริการ 400,000 ค่าใช้จ่ายขาย 200,000
และค่าใช้จ่ายบริหาร 150,000
```

## 💡 ตัวอย่างการใช้งานใน Claude Desktop

เมื่อตั้งค่าเรียบร้อยแล้ว คุณสามารถถามคำถามใน Claude Desktop เช่น:

### การถามเกี่ยวกับข้อมูล:

**Q:** "มีข้อมูลปีไหนบ้างในระบบ?"

**A:** Claude จะเรียก `get_filter_options` และบอกปีที่มีข้อมูล

---

**Q:** "สร้างรายงาน P&L ของปี 2024 เดือน 1-6 ทุกกลุ่มธุรกิจ"

**A:** Claude จะเรียก `get_report_data` พร้อมพารามิเตอร์ที่เหมาะสม

---

**Q:** "เปรียบเทียบรายได้ Q1 กับ Q2 ของปี 2024"

**A:** Claude จะเรียก `get_report_data` 2 ครั้ง (Q1 และ Q2) แล้ววิเคราะห์เปรียบเทียบให้

---

**Q:** "กลุ่มธุรกิจไหนมี EBITDA margin สูงสุดในปี 2024?"

**A:** Claude จะดึงข้อมูลแต่ละกลุ่มและวิเคราะห์ให้

### การคำนวณ:

**Q:** "ถ้ารายได้ 5,000,000 ต้นทุน 2,000,000 ค่าใช้จ่ายขาย 800,000 และบริหาร 500,000 จะได้ EBIT เท่าไร?"

**A:** Claude จะเรียก `calculate_metrics` และคำนวณให้

## 🔍 การตรวจสอบว่า MCP ทำงาน

1. เปิด Claude Desktop
2. ดูที่มุมล่างซ้าย ควรเห็นไอคอน 🔌 (plug icon)
3. คลิกไอคอนนั้น ควรเห็น "univer-report" ในรายการ
4. ถ้าเห็นสถานะ "Connected" แสดงว่าตั้งค่าสำเร็จ

## ❗ Troubleshooting

### ปัญหา: ไม่เห็น MCP server ใน Claude Desktop

**แก้ไข:**
1. ตรวจสอบว่า path ใน config file ถูกต้อง (ใช้ absolute path)
2. ตรวจสอบว่าไฟล์ `start_mcp_server.sh` มีสิทธิ์ execute (`chmod +x`)
3. Restart Claude Desktop
4. ลองรัน `start_mcp_server.sh` ด้วยมือเพื่อดู error

### ปัญหา: MCP server ขึ้น error เมื่อ Claude เรียกใช้

**แก้ไข:**
1. ตรวจสอบว่า virtual environment activate แล้ว
2. ตรวจสอบว่ามีไฟล์ CSV อยู่ใน `backend/data/`
3. ดู logs ของ Claude Desktop:
   - macOS: `~/Library/Logs/Claude/`
   - Windows: `%APPDATA%\Claude\logs\`
   - Linux: `~/.config/Claude/logs/`

### ปัญหา: Claude บอกว่าไม่มี tools

**แก้ไข:**
1. ตรวจสอบว่า MCP server version เป็น 1.20.0 หรือใหม่กว่า
2. ตรวจสอบว่า `server.py` มี decorator `@app.list_tools()` และ `@app.call_tool()`
3. Restart Claude Desktop

### ปัญหา: Permission denied บน Windows

**แก้ไข:**
Windows อาจบล็อกไฟล์ .sh ให้ใช้การตั้งค่าแบบนี้แทน:

```json
{
  "mcpServers": {
    "univer-report": {
      "command": "python",
      "args": ["C:\\path\\to\\univer\\backend\\mcp_server\\server.py"],
      "env": {
        "PYTHONPATH": "C:\\path\\to\\univer\\backend"
      }
    }
  }
}
```

## 📝 หมายเหตุเพิ่มเติม

- MCP server จะรันใน stdio mode ซึ่งหมายความว่า Claude Desktop จะเป็นผู้ควบคุมการเริ่มต้นและหยุดทำงาน
- ข้อมูลจะถูก cache ใน memory เพื่อประสิทธิภาพ หาก CSV มีการเปลี่ยนแปลง ให้ restart Claude Desktop
- MCP server ไม่มี authentication เพราะทำงานแบบ local-only
- การเรียกใช้ tools จะไม่ส่งข้อมูลออกจากเครื่องของคุณ (ยกเว้น conversation context ที่ส่งไป Claude API)

## 🔗 เอกสารอ้างอิง

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Claude Desktop MCP Setup](https://docs.anthropic.com/claude/docs/model-context-protocol)
- [MCP SDK Python](https://github.com/anthropics/mcp-python-sdk)

## 📧 ติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ กรุณาตรวจสอบ:
1. Logs ของ Claude Desktop
2. Output ของ `start_mcp_server.sh` เมื่อรันด้วยมือ
3. Version ของ dependencies ใน `requirements.txt`

---

**เวอร์ชัน:** 0.1.0
**อัพเดทล่าสุด:** 2025-11-22
**รองรับ:** Python 3.8+, MCP SDK 1.20.0+
